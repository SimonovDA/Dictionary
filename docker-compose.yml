version: "3"

services:
    src:
        restart: always
        environment:
            - MongoDbConnect=${MongoDbConnect}
        build:
            context: ./src
            dockerfile: Dockerfile
        ports: 
            - 8080:80
            - 443:443
